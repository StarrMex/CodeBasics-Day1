var addExclamation = function (input) {
  var newSentence = input + "!";
  return newSentence;
};

var main = function (input) {
  var myOutputValue = addExclamation(input);
  return myOutputValue;
};
